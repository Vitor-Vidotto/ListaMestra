{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-unused-vars": "off",  // Desabilita a regra de variáveis não utilizadas
    "no-unused-vars": "off"  // Desabilita a regra de variáveis não utilizadas (caso tenha conflito)
  }
}
